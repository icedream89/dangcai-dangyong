<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dangcai.system.mapper.UserRoleMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.dangcai.system.domain.UserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 批量插入用户角色关联 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO sys_user_role (user_id, role_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId}, NOW())
        </foreach>
    </insert>

    <!-- 根据用户ID删除角色关联 -->
    <delete id="deleteByUserId" parameterType="java.lang.Long">
        DELETE FROM sys_user_role
        WHERE user_id = #{userId}
    </delete>

    <!-- 根据角色ID删除用户关联 -->
    <delete id="deleteByRoleId" parameterType="java.lang.Long">
        DELETE FROM sys_user_role
        WHERE role_id = #{roleId}
    </delete>

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdsByUserId" parameterType="java.lang.Long" resultType="java.lang.Long">
        SELECT role_id
        FROM sys_user_role
        WHERE user_id = #{userId}
    </select>

</mapper>
